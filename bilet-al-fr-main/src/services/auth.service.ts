import { api, unwrap } from './api'; import type { User } from '@/types';
export const authService={register:(payload:{fullName:string;email:string;phone?:string;password:string})=>api.post('/auth/register',payload).then(unwrap<{user:User;accessToken:string}>),login:(payload:{email:string;password:string;rememberMe?:boolean})=>api.post('/auth/login',payload).then(unwrap<{user:User;accessToken:string}>),logout:()=>api.post('/auth/logout').then(unwrap),refresh:()=>api.post('/auth/refresh').then(unwrap<{user:User;accessToken:string}>),forgotPassword:(email:string)=>api.post('/auth/forgot-password',{email}).then(unwrap),resetPassword:(payload:{token:string;password:string})=>api.post('/auth/reset-password',payload).then(unwrap),verifyEmail:(token:string)=>api.post('/auth/verify-email',{token}).then(unwrap)};
